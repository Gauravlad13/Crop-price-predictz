{
  "name": "agropredict-backend",
  "version": "1.0.0",
  "description": "Agropredict backend API",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "cd ../frontend && npm install && npm run build || true"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "pg": "^8.20.0"
  },
  "devDependencies": {},
  "engines": {
    "node": "20.x"
  }
}
